// Custom icon static resources
//
// `style-icons-auto.ts` is regenerated from the templates by `ng g ng-alain:plugin icon`; this file
// is for icons that scan cannot see — ones named as plain strings in TypeScript:
//   - the menu tree in `StartupService.viaMock()` / `assets/tmp/app-data.json`
//   - `LAYOUT_MODE_META` in `core/layout/layout-mode.service.ts`
//   - `PortalComponent.fallbackIcon`, used for menu entries that declare no icon

import {
  AppstoreOutline,
  BugOutline,
  CloudServerOutline,
  FileSearchOutline,
  LayoutOutline,
  MenuOutline,
  StopOutline,
  WarningOutline
} from '@ant-design/icons-angular/icons';

export const ICONS = [
  AppstoreOutline,
  BugOutline,
  CloudServerOutline,
  FileSearchOutline,
  LayoutOutline,
  MenuOutline,
  StopOutline,
  WarningOutline
];
